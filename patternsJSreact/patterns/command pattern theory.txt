 the command pattern = an object is used to encapsulate (wrap) (zawierac) all information needed to perform an action or trigger an event at a later time

for example order. make order, id order, reset order. example below